import { StyleSheet } from "react-native";

export const MenuTopBarStyles = StyleSheet.create({
  iconContainer: {
    marginTop: 18,
    flexDirection: "row",
    alignItems: "center",
  },
  coffeeContainer: {
    left: 220,
  },
  backArrowContainer: {
    opacity: 0.8,
    left: 28,
  },
});
